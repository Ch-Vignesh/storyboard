## What this changes

<!-- One or two sentences. Link the requirement, e.g. FR-6.4, NFR-3, OD-2. -->

Requirement:

## Checklist

- [ ] The change is scoped to one requirement or one fix.
- [ ] User-facing text follows the vocabulary rules (`pnpm check-vocabulary` passes).
- [ ] Schema changes ship with a migration and a seed update.
- [ ] Tests cover the behaviour; `pnpm test` passes locally.
- [ ] No AI model is called anywhere in the product (docs/01-srs.md, principle 1.3.1).
